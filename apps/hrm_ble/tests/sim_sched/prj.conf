# SPDX-License-Identifier: Apache-2.0
#
# Nothing but ztest. The unit under test is a pure function - see the
# CMakeLists.txt beside this file - so there is no radiant, no backend, no
# profile and no BLE in this image, and adding any of them would only make the
# test slower and its failures less specific.
CONFIG_ZTEST=y
