# SPDX-License-Identifier: Apache-2.0

cmake_minimum_required(VERSION 3.20.0)

# The same ANT+ dongle as apps/dongle, plus Thread/Matter rebroadcast and the
# P3/P4 coexistence bench instruments (ble_coex_load.c, thread_coex_load.c)
# that apps/dongle deliberately carries none of - see that app's own
# CMakeLists.txt header for why splitting the two apart, rather than a
# Kconfig variant of one, is the point.
#
# The sysbuild-cache reader and the RADIANT_BACKEND selection/assertion
# machinery are shared with every other RadiANT app (apps/dongle,
# apps/hrm_ble) rather than copy-pasted - see cmake/radiant_backend.cmake for
# the sysbuild mechanics this depends on. The ANT_RADIO axis (sdk_ant vs core
# vs stub) is the ANT-serial-dongle-specific half of the same idea, and lives
# in apps/common/ instead, for the reason that file's own header gives.
include("${CMAKE_CURRENT_SOURCE_DIR}/../../cmake/radiant_backend.cmake")
include("${CMAKE_CURRENT_SOURCE_DIR}/../common/ant_radio_backend.cmake")

# radiant is appended unconditionally, same reasoning as apps/dongle's own
# CMakeLists.txt: it is in this repository and always is, so CONFIG_RADIANT
# gates it rather than the module's presence gating anything.
#
# THE P4 GATE NEEDS -DRADIANT_BACKEND=nrf ON THE COMMAND LINE, and the default
# here is still "null" - the same cheap-compile-check posture as apps/dongle,
# not "nrf" - because a script whose job is producing a bench image
# (scripts/build_p4.ps1) already passes -DRADIANT_BACKEND=nrf explicitly and
# reads the .config back to prove it landed; a default that silently disagreed
# with what a plain `west build` produced would be one more place for the
# arbitrated build to quietly become the null one.
radiant_select_backend("${CMAKE_CURRENT_SOURCE_DIR}/../../radiant" "null" RADIANT_BACKEND_CONF)
ant_select_radio("${CMAKE_CURRENT_SOURCE_DIR}" "" "${RADIANT_BACKEND_CONF}" ANT_RADIO_CONF)

# Write only on a change - see apps/dongle/CMakeLists.txt for the full
# rationale (CMAKE_CONFIGURE_DEPENDS churn on every configure otherwise).
set(ANT_RADIO_CONF_FILE "${CMAKE_CURRENT_BINARY_DIR}/ant_radio_backend.conf")
set(ANT_RADIO_CONF_EXISTING "")
if(EXISTS "${ANT_RADIO_CONF_FILE}")
  file(READ "${ANT_RADIO_CONF_FILE}" ANT_RADIO_CONF_EXISTING)
endif()
if(NOT ANT_RADIO_CONF_EXISTING STREQUAL ANT_RADIO_CONF)
  file(WRITE "${ANT_RADIO_CONF_FILE}" "# Generated by CMakeLists.txt from -DANT_RADIO. Do not edit; do not commit.\n${ANT_RADIO_CONF}")
endif()

find_package(Zephyr REQUIRED HINTS $ENV{ZEPHYR_BASE})
project(radiant_dongle_thread)

# See apps/common/ant_radio_backend.cmake's ant_assert_radio() - only
# relevant when ANT_RADIO chose radiant in the first place.
ant_assert_radio("radiant_dongle_thread")

target_sources(app PRIVATE src/main.c)

# thread_coex_load.h and ble_coex_load.h sit at this application's own root
# (not under src/) - see the file-placement note in the reorg plan - so
# main.c's quoted #include needs this directory on its search path.
target_include_directories(app PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}")

# ── apps/common: the antr_* seam, the serial bridge and its transports ─────
#
# One source list, added fresh into this application's own image - see that
# file's own header for why it is include()d rather than add_subdirectory()'d.
# CONFIG_ANT_DONGLE_TRANSPORT_* (set by the choice in Kconfig) and
# CONFIG_ANT_DONGLE_RADIO_*/CONFIG_RADIANT_ANTR_API (set above by
# ant_select_radio() and by apps/common/Kconfig.antr_api's default) together
# decide what actually compiles from it into this image.
include("${CMAKE_CURRENT_SOURCE_DIR}/../common/sources.cmake")

# ── The P3/P4 coexistence bench instruments ─────────────────────────────────
#
# The one thing that makes this application apps/dongle_thread rather than a
# second copy of apps/dongle: a second wireless stack that actually wants the
# radio. Off by default in every shipping image (see each Kconfig's own
# help), so these two lines are inert everywhere except a bench build that
# explicitly turns one on.
target_sources_ifdef(CONFIG_ANT_DONGLE_BLE_COEX_LOAD app PRIVATE
  "${CMAKE_CURRENT_SOURCE_DIR}/ble_coex_load.c")
target_sources_ifdef(CONFIG_ANT_DONGLE_THREAD_COEX_LOAD app PRIVATE
  "${CMAKE_CURRENT_SOURCE_DIR}/thread_coex_load.c")

# ── The RadiANT bridge: the sample bus and everything on it ─────────────────
#
# docs/radiant-bridge.md sections 3-6 and 9. Five files from the module's
# src/bridge/ directory plus this application's own half under src/.
#
# The shape is copied from radiant/tests/CMakeLists.txt, which is the only
# working precedent in the tree for consuming this directory, and the
# zephyr_linker_sources() line below is the part of it that is NOT optional.
#
# RADIANT_SINK_DEFINE() (radiant_bridge.h) uses STRUCT_SECTION_ITERABLE, which
# emits each sink into a section named after the struct type. Emitting into a
# section and having that section PLACED - with the _radiant_sink_list_start /
# _end symbols STRUCT_SECTION_FOREACH() walks between - are two different
# things, and radiant_sink has no in-tree linker fragment of its own the way
# shell commands and bt_conn_cb do. Omit the line and the best case is an
# undefined reference at the final link; the worse case is that it links and
# STRUCT_SECTION_FOREACH() silently iterates ZERO sinks, which is not a
# compile error, not a link error, and looks from the console exactly like
# "no sensors in range". src/bridge_pump.c logs the count at boot for that
# reason: "bridge: 0 sinks" is the failure announcing itself.
#
# radiant_rd_adapter.c is deliberately NOT here. It compiles and is tested
# (radiant/tests builds it), but the running-dynamics adapter has no bench
# evidence behind it yet, and linking it would make an untested path look like
# a shipped one.
#
# radiant_matter.c USED TO BE EXCLUDED BESIDE IT, FOR A REASON THAT HAS EXPIRED.
# The old comment here said Matter needs connectedhomeip, "which is a
# flash-budget spike this application has not run". Package E1 ran it
# (docs/matter-e1-readback.md) and package E3 vendored the bridge core, so the
# file is now compiled - but on ITS OWN Kconfig symbol
# (CONFIG_ANT_DONGLE_MATTER_MAP), not on CONFIG_CHIP. radiant_matter.c contains
# no CHIP call at all: it is the section 8.1 field-type -> cluster map plus a
# __weak radiant_matter_attr_write(), and it compiles identically with or
# without connectedhomeip in the workspace. Tying it to CONFIG_CHIP would have
# made a pure-C lookup table unbuildable on the nRF52840 Matterless escape
# hatch (docs/radiant-bridge.md section 11) for no reason.
if(CONFIG_RADIANT_BRIDGE)
  target_sources(app PRIVATE
    "${CMAKE_CURRENT_SOURCE_DIR}/../../radiant/src/bridge/radiant_bridge.c"
    "${CMAKE_CURRENT_SOURCE_DIR}/../../radiant/src/bridge/radiant_binding.c"
    "${CMAKE_CURRENT_SOURCE_DIR}/../../radiant/src/bridge/radiant_hr_adapter.c"
    "${CMAKE_CURRENT_SOURCE_DIR}/../../radiant/src/bridge/radiant_rules.c"
    # Liveness lands with the bus, not after a sink. RADIANT_SAMPLE_STALE has
    # been defined and honoured since P6 with nothing producing it, and a sink
    # written against a bus that never marks anything stale grows that
    # assumption into itself.
    "${CMAKE_CURRENT_SOURCE_DIR}/../../radiant/src/bridge/radiant_liveness.c"
    # The application half: the drain pump and the 1 Hz liveness clock.
    "${CMAKE_CURRENT_SOURCE_DIR}/src/bridge_pump.c"
  )
  target_include_directories(app PRIVATE
    "${CMAKE_CURRENT_SOURCE_DIR}/../../radiant/src/bridge"
    "${CMAKE_CURRENT_SOURCE_DIR}/src")

  # ── The Matter plan's packages A, B and C ──────────────────────────────
  #
  # Three more adapters and the page codecs behind them.
  #
  # NOT UNDER CONFIG_RADIANT_BRIDGE, AND THAT IS THE WHOLE POINT OF THE
  # SYMBOL. These files were added directly to the list above once, and
  # docs/matter-e2-regression.md measured what that cost: bridge.conf is one
  # of the five docs/radiant-bridge.md section 7.4 coexistence arms and it
  # gained 2 564 B of flash, 1 472 B of RAM and 22 defined symbols - plus a
  # changed radio schedule, via the rewritten src/self_channels.c, which is
  # the thing section 7.4 actually measures. CONFIG_ANT_DONGLE_PROFILES_EXTRA
  # defaults n and follows CONFIG_ANT_DONGLE_MATTER, exactly as
  # CONFIG_ANT_DONGLE_MATTER_MAP below already does and for the same reason.
  # See that symbol's help in Kconfig; do not "simplify" this back up into
  # the block above.
  #
  # Two things about the list itself are load-bearing:
  #
  # 1. THE COMMON-PAGE PAIR IS NOT OPTIONAL AND IS NOT PER-PROFILE.
  #    ANT+ Common Data Pages Rev 3.1 Table 4-1 keys pages 0x40-0x5D on the
  #    channel's TRANSMISSION TYPE, not on the device type, so the common
  #    decoder runs on every bound channel alongside whichever profile
  #    adapter owns it - see src/rx_tap.c. Omitting it does not disable a
  #    feature, it makes every weather-reporting sensor silent.
  #
  # 2. profile_power_decode.c, NOT profile_power.c. The latter is the
  #    MASTER - page rotation, the ANT+ compat layer, RadiANT beacon
  #    insertion - so it calls profile_sched.c and profile_compat.c, and
  #    profile_compat.c reaches radiant_sec. Linking it here to obtain one
  #    decoder would pull the entire transmit-side chain into an image
  #    whose whole job is to listen, and would let a linker decision nobody
  #    reviewed make a receive-only dongle capable of transmitting. See
  #    that file's header; the split exists for this line.
  target_sources_ifdef(CONFIG_ANT_DONGLE_PROFILES_EXTRA app PRIVATE
    "${CMAKE_CURRENT_SOURCE_DIR}/../../radiant/src/bridge/radiant_power_adapter.c"
    "${CMAKE_CURRENT_SOURCE_DIR}/../../radiant/src/bridge/radiant_env_adapter.c"
    "${CMAKE_CURRENT_SOURCE_DIR}/../../radiant/src/bridge/radiant_common_adapter.c"
    "${CMAKE_CURRENT_SOURCE_DIR}/../../radiant/src/profiles/profile_common.c"
    "${CMAKE_CURRENT_SOURCE_DIR}/../../radiant/src/profiles/profile_power_decode.c"
    "${CMAKE_CURRENT_SOURCE_DIR}/../../radiant/src/profiles/profile_fec.c"
    "${CMAKE_CURRENT_SOURCE_DIR}/../../radiant/src/profiles/profile_env.c")

  # The adapters include their page codec's header rather than re-deriving
  # the bit splits, exactly as radiant_rd_adapter.c already did in
  # radiant/tests. This application listed no profile source and no profile
  # include path before packages A-C, and with the symbol off it lists
  # neither again - an include path is not free, because it is also what
  # src/self_channels.c's header cites as the reason its periods are
  # transcribed literals rather than the profile constants.
  if(CONFIG_ANT_DONGLE_PROFILES_EXTRA)
    target_include_directories(app PRIVATE
      "${CMAKE_CURRENT_SOURCE_DIR}/../../radiant/src/profiles")
  endif()

  zephyr_linker_sources(SECTIONS
    "${CMAKE_CURRENT_SOURCE_DIR}/../../radiant/src/bridge/radiant_bridge_sections.ld")

  # The Matter sink's data half: the section 8.1 map, the SI -> cluster unit
  # conversion and the endpoint allocation. See the note above the `if` for why
  # this is a symbol of its own rather than CONFIG_CHIP, and Kconfig for why the
  # symbol defaults to n even here - the five existing coexistence arms have to
  # stay byte-identical, and bridge.conf is one of them.
  target_sources_ifdef(CONFIG_ANT_DONGLE_MATTER_MAP app PRIVATE
    "${CMAKE_CURRENT_SOURCE_DIR}/../../radiant/src/bridge/radiant_matter.c")
endif()

# ── The CHIP stack and the vendored Matter bridge core (package E3/E7) ──────
#
# EVERYTHING MATTER IS INSIDE THIS ONE `if`, and that is a requirement rather
# than tidiness. gate.conf, thread.conf, thread_sed.conf, coex.conf and
# bridge.conf carry the P3/P4 coexistence baselines in
# docs/radiant-bridge.md section 7.4, and those numbers describe the images
# they were taken on. CONFIG_CHIP is n in all five (matter_sysbuild.conf is
# named on the command line, never picked up automatically - see that file's
# header), so not one line below runs for them and their images are unchanged
# by construction rather than by care.
#
# WHY IT IS HERE AND NOT BEFORE find_package(Zephyr). The preamble at the top
# of this file - radiant_backend.cmake and ant_radio_backend.cmake - only
# generates .conf fragments and appends module paths; it creates no target and
# reads no Kconfig. Everything below needs both `app` to exist and
# CONFIG_* to be readable, so it belongs after project(). The two NCS
# applications that do this - nrf/applications/matter_bridge/CMakeLists.txt
# and nrf/samples/matter/template/CMakeLists.txt - are laid out exactly this
# way and this block follows them line for line.
#
# THE APPLICATION DOES NOT BECOME C++. Only the .cpp files listed below are,
# and enable-gnu-std.cmake only appends C++ flags. src/main.c and everything
# under apps/common stay C, and CONFIG_STD_CPP17 (matter.conf, with a long
# note on why it is not optional) applies to the C++ half alone.
if(CONFIG_CHIP)
  # -std=gnu++17 rather than -std=c++17 for the CHIP sources. Without it,
  # connectedhomeip does not compile - see matter.conf's CONFIG_STD_CPP17 note
  # for the 40-line error that looks like a broken toolchain and is not.
  include(${ZEPHYR_CONNECTEDHOMEIP_MODULE_DIR}/config/nrfconnect/app/enable-gnu-std.cmake)

  # NCS's Matter sample-common sources: board/led_widget, the task executor,
  # matter_init, the event handler and the code-driven cluster registry (plus
  # its cluster_init.ld linker fragment, which this file therefore does NOT
  # repeat). Every Matter sample and application in NCS includes this and the
  # bridge core expects the persistent-storage and binding pieces of it to be
  # present.
  #
  # board.cpp IS COMPILED AND DOES NOT DRIVE ANY LED HERE. Both its LED paths
  # are inside `#ifdef CONFIG_DK_LIBRARY` and this arm does not enable
  # DK_LIBRARY, so the collision matter.conf's CONFIG_NCS_SAMPLE_MATTER_LEDS=n
  # was written to prevent (NCS driving DK LEDs 0-3 against
  # apps/common/ant_dongle_main.c's led0) cannot happen twice over. The =n line
  # stays because it states the decision; this comment states the measurement.
  include(${ZEPHYR_NRF_MODULE_DIR}/samples/matter/common/cmake/source_common.cmake)

  # ncs_configure_data_model() and ncs_get_zap_parent_dir(), both of which read
  # CONFIG_NCS_SAMPLE_MATTER_ZAP_FILE_PATH - set in matter.conf to this
  # application's own src/matter/default_zap/bridge.zap.
  include(${ZEPHYR_NRF_MODULE_DIR}/samples/matter/common/cmake/data_model.cmake)
  include(${ZEPHYR_NRF_MODULE_DIR}/samples/matter/common/cmake/zap_helpers.cmake)
  ncs_get_zap_parent_dir(ZAP_PARENT_DIR)

  target_include_directories(app PRIVATE
    "${CMAKE_CURRENT_SOURCE_DIR}/src/matter"
    "${CMAKE_CURRENT_SOURCE_DIR}/src/matter/core"
    "${CMAKE_CURRENT_SOURCE_DIR}/src/matter/core/util"
    # zap-generated/ lands on the include path through its PARENT directory,
    # because the generated sources include it as <zap-generated/...>. This is
    # the line that makes the committed ZAP output the thing the build reads;
    # ncs_configure_data_model() adds the same directory to the separate
    # matter-data-model target.
    ${ZAP_PARENT_DIR}
  )

  target_sources(app PRIVATE
    # The vendored bridge core. src/matter/README.md records the source path,
    # the NCS version and everything that was left behind - the whole BLE
    # central provider first among it.
    "${CMAKE_CURRENT_SOURCE_DIR}/src/matter/core/bridge_manager.cpp"
    "${CMAKE_CURRENT_SOURCE_DIR}/src/matter/core/matter_bridged_device.cpp"
    "${CMAKE_CURRENT_SOURCE_DIR}/src/matter/core/bridge_storage_manager.cpp"
    "${CMAKE_CURRENT_SOURCE_DIR}/src/matter/core/bridged_device_data_provider.cpp"
    # NOT vendored, referenced in place, exactly as matter_bridge does:
    # BridgeManager::Init() calls Nrf::Matter::BindingHandler::Init() and
    # BridgeManager::HandleCommand() builds its BindingData. It is NCS's file
    # and there is no reason for this repository to own a copy.
    "${ZEPHYR_NRF_MODULE_DIR}/samples/matter/common/src/binding/binding_handler.cpp"
  )

  # ── Packages E4, E5 and E6: the ANT+ half, the device types and main() ────
  #
  # Everything here is THIS repository's, not vendored - src/matter/README.md's
  # provenance table covers core/ and default_zap/ and nothing below.
  #
  # ONE OF THESE FIVE FILES IS C, AND THAT IS DELIBERATE.
  # ant/ant_matter_sink.c carries the RADIANT_SINK_DEFINE() that gives the glue
  # a binding_changed callback and a view of RADIANT_SAMPLE_STALE. That macro is
  # STRUCT_SECTION_ITERABLE plus a designated initialiser; designated
  # initialisers are only a GNU extension in C++17 and the iterable-section
  # machinery wants an unmangled symbol in a section named after the struct.
  # Both happen to work under -std=gnu++17 and neither is guaranteed, so the
  # nine lines that need them are C. See that file's header.
  #
  # NESTED INSIDE CONFIG_RADIANT_BRIDGE, exactly like the trap-15 assertions
  # below and for the same mechanical reason: ant_bridge.cpp includes
  # radiant_matter.h and ant_matter_sink.c includes radiant_bridge.h, and
  # radiant/src/bridge only reaches this target's include path from the
  # CONFIG_RADIANT_BRIDGE block further up. It is not a second opinion about
  # whether Matter is wanted - matter.conf sets both symbols and CONFIG_CHIP
  # without the sample bus would be a bridge with nothing to bridge.
  #
  # Note what is NOT the guard: CONFIG_ANT_DONGLE_MATTER, which is what main.c
  # tests. That symbol `depends on CHIP` and has no default, so a CHIP build
  # that left it unset compiles these files, links them, and finds nothing
  # calls ant_matter_start() - a dead sink and a --gc-sections discard. The
  # other order (guarding the sources and not the call) is the one that fails,
  # at link.
  if(CONFIG_RADIANT_BRIDGE)
    target_sources(app PRIVATE
      "${CMAKE_CURRENT_SOURCE_DIR}/src/matter/ant/ant_bridge.cpp"
      "${CMAKE_CURRENT_SOURCE_DIR}/src/matter/ant/ant_matter_sink.c"
      "${CMAKE_CURRENT_SOURCE_DIR}/src/matter/bridged_device_types/occupancy_sensor.cpp"
      "${CMAKE_CURRENT_SOURCE_DIR}/src/matter/bridged_device_types/measurement_sensor.cpp"
      # E6. It also carries the one LOG_MODULE_REGISTER(app) this image has:
      # every vendored core file and every NCS sample-common source does
      # LOG_MODULE_DECLARE(app), and upstream registers it in the main.cpp that
      # E3 deliberately did not vendor.
      "${CMAKE_CURRENT_SOURCE_DIR}/src/matter/matter_app.cpp"
      # Trap 15's static_assert. Same include-path reason as the four above;
      # not a statement that the assertion is optional.
      "${CMAKE_CURRENT_SOURCE_DIR}/src/matter/bridge_core_asserts.cpp")
  endif()

  # Compiles src/matter/default_zap/zap-generated/ into the matter-data-model
  # target, with BYPASS_IDL - so chip_zapgen never runs and the COMMITTED
  # directory is what the image contains. See src/matter/README.md; this is
  # trap 13 and it is the reason .zap, .matter and zap-generated/ were copied
  # across together and unmodified.
  ncs_configure_data_model()
endif()

target_sources_ifdef(CONFIG_ANT_DONGLE_RX_TAP app PRIVATE
  "${CMAKE_CURRENT_SOURCE_DIR}/src/rx_tap.c")

# CONFIG_ANT_DONGLE_SELF_CHANNELS is an int, so target_sources_ifdef() (which
# tests for truthiness) would fire on 0 as well - Kconfig writes
# CONFIG_ANT_DONGLE_SELF_CHANNELS=0 rather than leaving it unset for an int
# symbol. Compare explicitly.
if(CONFIG_ANT_DONGLE_SELF_CHANNELS GREATER 0)
  target_sources(app PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/self_channels.c")
endif()

target_sources_ifdef(CONFIG_ANT_DONGLE_MQTT_SINK app PRIVATE
  "${CMAKE_CURRENT_SOURCE_DIR}/src/mqtt_sink.c")

# ── The hostless node's identity (compat-C6) ────────────────────────────────
#
# Same wiring as apps/dongle's own CMakeLists.txt - see that file's comment
# for the full rationale. Off by default here too: this application's epoch
# authority is still the PC behind it, same as the plain dongle.
if(CONFIG_RADIANT_NODE_IDENT)
  target_sources(app PRIVATE ../../radiant/src/node/node_ident.c)
  target_include_directories(app PRIVATE ../../radiant/src/node)
endif()
if(CONFIG_RADIANT_NODE_NVM_SETTINGS)
  target_sources(app PRIVATE ../../radiant/src/node/node_nvm_settings.c)
endif()
