# RadiANT heart-rate strap.
#
# Nothing here is shared with the dongle's prj.conf on purpose, and for the same
# reason sim/ says it: this build has no USB, no serial bridge and no
# partition-manager constraints. It is a sensor. What it does share with the
# dongle - and sim/ does not - is the whole radio stack, because that is the
# point of the application.

CONFIG_STRAP=y

# radiant_core and its antr_* adapter. The backend itself comes from
# -DRADIANT_BACKEND via the generated fragment in CMakeLists.txt, so that a
# null-radio build is a deliberate choice rather than a default nobody noticed.
CONFIG_RADIANT_CORE=y
CONFIG_RADIANT_CORE_ANTR_API=y

# The identity record and its storage. ADR 0009 makes the device number a
# property of the node rather than of the image, and settings-backed NVM is the
# same implementation the dongle uses.
#
# RADIANT_SEC is not optional here and not a feature choice: src/node/Kconfig
# says `depends on RADIANT_SEC` because everything in node_ident derives keys,
# down to the boot counter's epoch. A strap without it is a strap with no
# identity, which is the one thing ADR 0009 says a node must have.
CONFIG_RADIANT_SEC=y
CONFIG_RADIANT_NODE_IDENT=y
CONFIG_RADIANT_NODE_NVM_SETTINGS=y
CONFIG_SETTINGS_NVS=y

CONFIG_HEAP_MEM_POOL_SIZE=2048

# Logs out the DK's first VCOM. Not RTT: RTT needs SEGGER's JLinkARM DLL
# installed, whereas the VCOM is carried by the cable that already programs the
# board.
CONFIG_LOG=y
CONFIG_LOG_MODE_DEFERRED=y
CONFIG_SERIAL=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y
CONFIG_LOG_BACKEND_UART=y

# Halt on a fault rather than rebooting into it. A sensor that resets in a loop
# still transmits between resets, which reads as an intermittent radio fault
# from the receiving end - the most expensive kind of symptom to chase, and one
# this project has already paid for twice.
CONFIG_RESET_ON_FATAL_ERROR=n
CONFIG_THREAD_NAME=y
