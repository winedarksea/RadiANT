# The module. Its choice default (RADIANT_BACKEND_NULL) needs nothing this
# application does not already have - no radio, no board-specific devicetree
# node - which is exactly why it is the right backend for a build meant to
# prove the module's SOURCE is self-contained, not its radio.
CONFIG_RADIANT=y

CONFIG_LOG=y
